// Temporary fallback for sandbox environment without network access
// In a real environment, you would use Google Fonts imports

const inter = {
  variable: "--font-inter",
  className: "font-inter"
};

const poppins = {
  variable: "--font-poppins", 
  className: "font-poppins"
};

export { inter, poppins };
